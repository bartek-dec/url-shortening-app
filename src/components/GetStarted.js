import React from 'react';

const GetStarted = () => {
    return (
        <section className='get-started'>
            <h2 className='get-started-heading'>Boost your links today</h2>
            <button type='button' className='btn'>Get Started</button>
        </section>
    );
};

export default GetStarted;